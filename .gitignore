node_modules/
.next/
coverage/
.DS_Store
*.log
.env*
!.env.example

.vercel

# Claude Code agent worktrees (temporary, auto-cleaned)
.claude/worktrees/

# PAI memory (local only)
MEMORY/

# Brainstorm companion mockups (local only)
.superpowers/

# Git worktrees
.worktrees/
tsconfig.tsbuildinfo

# local data & config — never commit
stellar-toolkit.db
stellar-toolkit.db-*
.claude/settings.local.json
